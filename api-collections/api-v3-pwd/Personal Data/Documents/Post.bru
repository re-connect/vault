meta {
  name: Post
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v3/documents
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  file: undefined
}

auth:bearer {
  token: {{oauth2_token}}
}
