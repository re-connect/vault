{{ include('v2/app/common/personal_data_detail.html.twig', {
    'information': {
        'note.nomLabel': note.nom,
        'note.contenuLabel': note.contenu,
        'add_date': note.createdAt|format_datetime(),
    },
    'editPath': path('note_edit', {'id': note.id}),
    'cancelPath': path('note_list', {'id': beneficiary.id}),
}) }}
