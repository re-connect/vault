<nav class="navbar navbar-light bg-white mx-0 mt-75 vault-nav">
    <div class="container-fluid max-width-lg px-0 my-1">
        <div class="col-12 col-md-6 text-primary font-size-1-3 ps-4 ps-lg-2">
            <i class="fas fa-user-friends me-2"></i>
            <span class="bold">{{ beneficiary.user.fullName|upper }}</span>
            <a class="btn btn-blue text-white ms-2 py-1" href="{{ path('re_membre_beneficiaires') }}">
                {{ 'leave'|trans }}
            </a>
        </div>
        <div class="col-12 col-md-6 col-lg-4">
            <ul class="list-group list-group-horizontal row">
                {% for key, item in itemsList %}
                    {{ include('v2/vault/nav/pro/_pro_nav_item.html.twig', {
                        'item': key,
                        'title': item.title,
                        'routeName': item.routeName,
                        'image': item.image
                    }) }}
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>
