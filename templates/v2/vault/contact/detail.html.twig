{{ include('v2/vault/components/_personal_data_detail.html.twig', {
    'information': {
        'name': contact.nom,
        'firstname': contact.prenom,
        'add_date': contact.createdAt|format_datetime(),
        'phone': contact.telephone,
        'email': contact.email,
        'comment': contact.commentaire,
        'association': contact.association,
    },
    'editPath': path('contact_edit', {'id': contact.id}),
    'cancelPath': path('list_contacts', {'id': beneficiary.id}),
}) }}
