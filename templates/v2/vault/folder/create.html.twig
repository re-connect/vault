{% extends 'v2/vault/standard_layout.html.twig' %}

{% block metas %}
    <meta name="turbo-cache-control" content="no-preview">
{% endblock %}

{% block content %}
    <div {{ stimulus_controller('autocomplete', {'source': autocompleteNames}) }}>
        {{ include('v2/vault/form/_personal_data_form.html.twig', {
            'title': 'document.creerUnDossier',
            'icon' : 'fas fa-folder-plus',
            'cancelPath': path('list_documents', {'id': beneficiary.id}),
        }) }}
    </div>
{% endblock %}
