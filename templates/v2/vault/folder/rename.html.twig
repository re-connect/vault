{% extends 'v2/vault/standard_layout.html.twig' %}

{% block metas %}
    <meta name="turbo-cache-control" content="no-preview">
{% endblock %}

{% block content %}
    {{ include('v2/vault/form/_personal_data_form.html.twig', {
        'title': 'rename',
        'cancelPath': folder.dossierParent
        ? path('folder', {'id': folder.dossierParent.id})
        : path('list_documents', {'id': folder.beneficiaire.id})
    }) }}
{% endblock %}
