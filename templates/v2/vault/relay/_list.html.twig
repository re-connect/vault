<div class="container mx-auto w-75">
    {% for relay in relays %}
        {{ include('v2/vault/relay/_card.html.twig', {'relay': relay}) }}
    {% endfor %}
    {% for pendingRelay in pendingRelays %}
        {{ include('v2/vault/relay/_pending_card.html.twig', {'pendingRelay': pendingRelay}) }}
    {% endfor %}
</div>
