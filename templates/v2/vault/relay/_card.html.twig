<div class="text-primary font-size-1-3 mb-5">
    <p class="bold mb-1">{{ relay.nom }}</p>
    <p class="mb-1">
        <i class="fa fa-home text-green me-1"></i>
        {{ include('v2/vault/relay/_address.html.twig', {relay}) }}
    </p>
    <a href="{{ path('relay_leave', {'id': beneficiary.id, 'relayId': relay.id}) }}" class="btn btn-primary text-white text-center mt-2 col mx-1">
        <i class="fa fa-times text-white me-1"></i>
        {{ 'leave_center'|trans }}
    </a>
</div>
