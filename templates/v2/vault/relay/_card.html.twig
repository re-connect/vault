{% set center = relay.centre %}

{{ include('v2/common/_list_card.html.twig', {
    disabled: relay.bValid == false,
    title: center.nom,
    icon: 'hotel',
    content: center.adresse,
    action: path('leave_relay', {id: center.id}),
    actionLabel: 'leave_center'|trans
}) }}
