{{ include('v2/vault/components/_personal_data_detail.html.twig', {
    'information': {
        'name': document.nom,
        'status': (document.bPrive ? 'private' : 'shared')|trans,
        'added_by': document.getCreatorUserFullName|upper,
        'add_date': document.createdAt|format_datetime(),
    },
    'editPath': path('document_rename', {'id': document.id}),
    'cancelPath': document.dossier
        ? path('folder', {'id': document.dossier.id})
        : path('list_documents', {'id': document.beneficiaire.id}),
    'isDocument': true,
}) }}
