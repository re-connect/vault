{{ include('v2/vault/components/_personal_data_detail.html.twig', {
    'information': {
        'title': note.nom,
        'note_content': note.contenu,
        'add_date': note.createdAt|format_datetime(),
    },
    'readHtml': true,
    'editPath': path('note_edit', {'id': note.id}),
    'cancelPath': path('list_notes', {'id': beneficiary.id}),
}) }}
