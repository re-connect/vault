{% extends 'v2/standard_layout.html.twig' %}
{% block metaTitle %}{{ 'invite_user_to_relay'|trans }}{% endblock %}

{% block delimiter %}{% endblock %}

{% block content %}
    <h5 class="my-4">
        {{ 'username'|trans }} :
        <span class="transform-uppercase text-blue">{{ user.username }}</span>
    </h5>
    {{ include('v2/user_creation/components/_relays_checkboxes.html.twig', {user})}}
    <div class="mt-5"></div>
    {{ include('v2/common/_button_row.html.twig', {
        color: 'blue',
        href: path('list_pro')
    }) }}
{% endblock %}
