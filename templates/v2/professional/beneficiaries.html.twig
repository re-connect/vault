{% extends 'v2/base.html.twig' %}

{% block body %}
    <div class="max-width-lg px-3 m-auto">
        <table class="table table-100 spaced-table mb-0 h-100 bg-white mt-75">
            <tbody>
            {% for beneficiary in beneficiaries %}
            <tr class="text-primary align-middle">
                <td class="bg-white borderless p-0 h-100 text-end">
                    <i class="fas fa-user fa-3x text-primary"></i>
                </td>
                <td class="bg-white borderless h-100">
                    <span>{{ beneficiary.user.prenom|capitalize }} {{ beneficiary.user.nom|upper }}</span>
                    <br>
                    <span class="text-grey">{{ beneficiary.user.username }}</span>
                </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
