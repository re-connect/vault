{{ form_start(form) }}
<div class="row">
    <div class="col-6 px-0">
        {{ form_widget(form.relay, {'attr': {'class': 'text-grey py-1'}}) }}
    </div>
    <div class="col-6 pe-0">
        <div class="input-group rounded-pill border bg-white z-0">
            <i class="fa fa-search text-grey p-2 input-group-text bg-white rounded-pill border-0"></i>
            {{ form_widget(form.search, {'attr': {'class': 'border-0 rounded-pill p-0 text-grey grey-placeholder'}}) }}
        </div>
    </div>
</div>
{{ form_end(form) }}
