{% extends 'v2/common/_layout.html.twig' %}

{% block metaTitle %}{{ 'add_si_siao_number'|trans }}{% endblock metaTitle %}
{% block title %}{{ 'add_si_siao_number'|trans }}{% endblock %}

{% block bodyContent %}
    <div class="container">
        <h2 class="pb-2 text-center">{{ beneficiary.fullName }}</h2>
        {{ form_start(form) }}
        <div class="row">
            <div class="d-flex align-items-center col-12 col-md-8 offset-md-2 col-lg-6 offset-lg-3">
                <div class="flex-grow-1">
                    {{ form_widget(form.number) }}
                </div>
                <div class="text-end ms-2">
                    {{ include('v2/common/_button_with_loader.html.twig', {'text': 'add'|trans}) }}
                </div>

            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
