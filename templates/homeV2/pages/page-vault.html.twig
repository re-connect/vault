{% extends 'homeV2/base.html.twig' %}
    {% block title %}{{ 'home_meta_title_vault'| trans }}{% endblock %}
    {% block meta_title %}{{ 'home_meta_title_vault'| trans }}{% endblock %}
{% block body %}

    {{ include('homeV2/sections/page-vault-hero-section.html.twig') }}

    {% for feature in features %}
        {{ include ('homeV2/sections/feature-vault-section.html.twig',
            {
                direction: loop.index is even ? 'row' : 'row-reverse',
                feature
            }) }}
    {% endfor %}

    {{ include('homeV2/templates/product-is-template.html.twig',{section_title: 'cfn_vault_is_title', product_is}) }}

    {{ include('homeV2/templates/contact-us-template.html.twig', {
        bg_class: 'bg-green',
        logo_file_name: 'watermark_vault.png',
        logo_alt: 'contact_us_alt_logo_reconnect'
    }) }}

{% endblock body %}
