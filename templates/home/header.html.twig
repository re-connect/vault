<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top border-bottom border-primary py-2 py-lg-0">
        <div class="container-fluid">
            <a target="_self" href="{{ path('re_main_accueil') }}">
                <img id="logo-header-reconnect" src="{{ asset('build/images/home/logo_reconnect_simple.png') }}"
                     alt="{{ 'alt_logo_reconnect'| trans }}">
            </a>
            <button class="navbar-toggler border border-primary"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbarNavAltMarkup"
                    aria-controls="navbarNavAltMarkup"
                    aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
                <div class="navbar-nav text-center font-size-1-3 d-flex justify-content-around">
                    <a target="_self" class="nav-item nav-link nav-links px-5"
                       href="{{ path('re_main_page_vault') }}">{{ 'header_link_cfn'|trans|raw }}</a>
                    <a target="_self" class="nav-item nav-link nav-links px-5"
                       href="{{ path('re_main_page_RP') }}">{{ 'header_link_rp'|trans|raw }}</a>
                    <div class="dropdown">
                        <a role="button"
                           class="nav-item nav-link nav-links px-5 dropdown-toggle"
                           data-bs-toggle="dropdown"
                           aria-expanded="false">
                            {{ 'digital_cares'|trans|nl2br }}
                        </a>
                        <ul class="dropdown-menu text-center border border-primary fs-5 rounded-0 mt-0">
                            <li>
                                <a target="_self" class="nav-item nav-link nav-links py-1 px-3"
                                   href="{{ path('digital_labs') }}">{{ 'header_link_digital_labs'|trans|raw }}</a>
                            </li>
                            <li><hr class="dropdown-divider border-primary mx-2"></li>
                            <li>
                                <a target="_self" class="nav-item nav-link nav-links py-1 px-3"
                                   href="{{ path('pro_learning') }}">{{ 'header_link_pro_learning'|trans|raw }}</a>
                            </li>
                        </ul>
                    </div>
                    <a target="_self" class="nav-item nav-link nav-links px-5"
                       href="{{ path('re_main_accueil') }}#qui-sommes-nous">{{ 'header_link_who_we_are'|trans|raw }}</a>
                    <a target="_blank" class="nav-item nav-link nav-links text-primary px-5"
                       href="https://medium.com/reconnect">{{ 'header_link_our_blog'|trans|raw }}</a>
                    <a target="_self" class="nav-item nav-link nav-links px-5"
                       href="{{ path('re_main_contactV2') }}">{{ 'header_link_contact'|trans|raw }}</a>
                </div>
            </div>
        </div>
    </nav>
</header>
